import {Route} from '@angular/router';
import { LandingFinanzasComponent } from './finanzas.component';

export const landingFinanzasRoutes: Route[] = [
    {
        path: '',
        component: LandingFinanzasComponent
    }
];
