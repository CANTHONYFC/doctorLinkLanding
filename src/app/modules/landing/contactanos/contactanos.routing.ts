import {Route} from '@angular/router';
import { LandingContactanosComponent } from './contactanos.component';

export const landingContactanosRoutes: Route[] = [
    {
        path: '',
        component: LandingContactanosComponent
    }
];
